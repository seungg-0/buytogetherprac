pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()

        //KakaoSDK repository
        maven { url 'https://devrepo.kakao.com/nexus/content/groups/public/' }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "buytogetherprac"
include ':app'
